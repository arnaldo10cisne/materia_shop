import classNames from "classnames";
import React from "react";
import styles from "./Header.module.scss";

export const Header = () => {
  return <div className={classNames(styles.Header)}>MATERIA SHOP HEADER</div>;
};
